using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    /* GameObject flag; //게임 오브젝트 flag
    void Start()
    {
        flag = GameObject.Find("flag"); // 게임 오브젝트 flag 찾기
        
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetMouseButtonDown(0)) //마우스 왼쪽 버튼을 눌렀으면
        {
            transform.position = flag.transform.position; //자동차 위치 깃발 위치로 변경
        }
    } */
}
